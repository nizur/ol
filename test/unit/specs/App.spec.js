import Vue from 'vue'
import App from 'src/App'

describe('App', () => {
  // Inspect the raw component options
  it('has the correct name', () => {
    expect(typeof Business.name).toBe('App')
  })
})
